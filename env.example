# ============================================
# DATABASE CONFIGURATION
# ============================================

# DATABASE_URL: Connection string for Prisma Client queries
# - For Supabase: Use connection pooling (port 6543) with pgbouncer=true
# - For local PostgreSQL: Use direct connection (e.g., postgresql://user:pass@localhost:5432/dbname)
# - For production: Use connection pooler for better performance
DATABASE_URL="postgresql://postgres.iqbyrlehypnkowtjzlwz:[YOUR-PASSWORD]@aws-1-eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# DIRECT_URL: Direct database connection for Prisma migrations
# - Required when using connection pooling (pgbouncer)
# - Use direct connection port (5432 for Supabase)
# - Only needed for `prisma migrate` and `prisma db push` commands
DIRECT_URL="postgresql://postgres.iqbyrlehypnkowtjzlwz:[YOUR-PASSWORD]@aws-1-eu-west-1.pooler.supabase.com:5432/postgres"

# ============================================
# AI CONFIGURATION
# ============================================

# OPENAI_API_KEY: OpenAI API key for draft generation
# - Required for the ingestion pipeline's GENERATING step
# - Get your API key from: https://platform.openai.com/api-keys
# - Model used: gpt-4-turbo-preview (configurable in ingestion-generator.ts)
OPENAI_API_KEY=your-openai-api-key-here

# ============================================
# APPLICATION CONFIGURATION
# ============================================

# NEXT_PUBLIC_BASE_URL: Base URL for the application (optional)
# - Used for internal API calls (e.g., regenerate endpoint calling generate endpoint)
# - Defaults to http://localhost:3000 in development
# - Set to your production domain in production (e.g., https://yourdomain.com)
# NEXT_PUBLIC_BASE_URL=http://localhost:3000

